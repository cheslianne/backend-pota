// Runtime config. In Docker/Railway this file is regenerated at container
// startup from the API_BASE_URL environment variable (see docker-entrypoint.sh).
// Locally (no Docker), leave this empty so scripts fall back to localhost.
window.API_BASE_URL = "";
